generator client {
  provider = "prisma-client-js"
  output   = "./generate-customer"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL_CUSTOMER")
}

model Customer {
  id   Int     @id @default(autoincrement())
  name String? @db.VarChar(255)
}
